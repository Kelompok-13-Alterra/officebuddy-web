import React from "react";
import ReactDOM from "react-dom/client";
import App from "./config/routing/App";
import '../src/config/routing/App.css'

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
